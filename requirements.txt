# Direct dependencies only. pip resolves the transitive tree.
#
# This file was previously a system-wide `pip freeze` -- 152 lines including
# pycairo, Brlapi and other packages this project never imports. pycairo alone
# broke every container build, because it needs cairo headers that no Django app
# here has any use for. Anything added below must correspond to an actual import
# in this repo, or to something Django loads by name from settings.py.

# --- Framework ---
Django>=6.0.3
djangorestframework>=3.17.0
djangorestframework_simplejwt>=5.5.1
asgiref>=3.11.1
gunicorn>=25.1.0

# --- Django plugins loaded by name from settings.py ---
django-cors-headers>=4.9.0
django-csp>=4.0
django-extensions>=4.1
django-permissions-policy>=4.28.0
django-ratelimit>=4.1.0
django-redis>=6.0.0
whitenoise>=6.12.0

# --- Data stores ---
psycopg2-binary>=2.9.11
redis>=7.4.0
meilisearch>=0.40.0

# --- Identity ---
# Passkeys, the second step of sign-in.
webauthn>=2.5.0
# Supabase is the only identity provider. firebase_admin remains for FCM push.
supabase>=2.28.3
PyJWT>=2.12.1
firebase_admin>=7.3.0

# --- Outbound ---
requests>=2.32.5
resend>=2.26.0
openai>=2.29.0

# --- PDF receipts ---
xhtml2pdf>=0.2.17
reportlab>=4.4.10

# --- Runtime config ---
python-dotenv>=1.2.2

# --- Tests ---
pytest>=9.0.2
pytest-django>=4.9.0
pytest-cov>=6.0.0
pytest-mock>=3.15.1
