[pytest]
DJANGO_SETTINGS_MODULE = djangoProject15.settings
python_files = test_*.py
testpaths = Alltechmanagement/tests
addopts = -ra --strict-markers
markers =
    live: hits a running server rather than the Django test client
